// Define variable
def tagjira = "PRUEB-2"

// Print variable
pipeline {
  agent any
  stages {
    stage ("Mostramos el valor de la variable: ") {
      steps {
        echo "El valor de la variable es: ${myVariable}"
      }
    }
  }
}
